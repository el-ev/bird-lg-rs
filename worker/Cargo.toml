[package]
name = "worker"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
common = { path = "../common" }
cloudflare_worker = { package = "worker", version = "0.8.0" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
