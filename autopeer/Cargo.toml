[package]
name = "autopeer"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
common = { path = "../common" }
ui_components = { package = "components", path = "../components" }
yew = { version = "0.21", features = ["csr"] }
