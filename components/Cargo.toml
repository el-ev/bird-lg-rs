[package]
name = "components"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
yew = { version = "0.21", features = ["csr"] }
web-sys = { version = "0.3", features = [
    "HtmlInputElement",
    "HtmlTextAreaElement",
    "KeyboardEvent",
] }
