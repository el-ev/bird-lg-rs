[package]
name = "components"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
unicode-width = "0.2"
web-sys = { version = "0.3", features = [
    "HtmlInputElement",
    "HtmlSelectElement",
    "HtmlTextAreaElement",
    "KeyboardEvent",
] }
yew = { version = "0.21", features = ["csr"] }
